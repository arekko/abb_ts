# import Error from '../../shared.graphql'

type Query {
  bye2: String
}

type LoginResponse {
  errors: [Error!],
  sessionId: String
}

type Mutation {
  login(email: String!, password: String!) : LoginResponse! 
}

